'use strict';

/* jasmine specs for routing go here */

xdescribe('Routing: routing...', function(){
   //test that correct template is loaded, and/or proper controller?
   //test all URL scenarios
        //at least one test for each page
        //a test for each view?
        //login/logout
   //test malformed URLs or URLs with missing components
   var router = module.$routeProvider;
  xit("should determine correct path from URL",
    inject(function(router) {
        expect(router);

    })
  );
    //htmlfilter = new 451orders.filter(html);


});